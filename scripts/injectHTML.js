module.exports = (then) => {
  // let now = Date.now()
  // const chunk = (then - now) / 100
  // let oValue = 0
  // while (now + chunk <= then) {
  //   now +=chunk
  //   oValue++
  // }
  // console.log(oValue)
  
}

/* <script>
    let now = Date.now();
    const chunk = (${then} - now) / 100;
    let oValue = 0;
    while (now + chunk <= ${then}) {
      now +=chunk;
      oValue++;
    };
    document.body.style.opacity = oValue
    </script>
    </body>
*/
